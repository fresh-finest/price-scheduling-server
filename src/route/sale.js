const express= require("express");
// const { fetchWeeklySalesMetrics } = require("../service/getReportService");

const router = express.Router();

//get /api/history/:schdeuleId

// router.route("/").get(fetchSalesMetricsByDay);
// router.route("/week").get(fetchWeeklySalesMetrics);


module.exports = router;